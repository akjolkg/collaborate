#bash script that checks if a given user exists by Leyla
![CDATA[ 
#!/bin/bash
user_name=$1
if [[ -n $(grep $user_name /etc/passwd) ]]
then
        echo "User $user_name exists!"
else
        echo "User $user_name doesn't exist"
fi
 ]]>
